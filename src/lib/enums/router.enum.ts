export enum routesURL {
  allFilms = "/",
  movieDetails = "/movie/:id",
  watchlist = "/watchlist",
  watchlistMovieDetails = ":id",
  registration = "/registration",
  login = "/login",
  notFound = "*",
}
